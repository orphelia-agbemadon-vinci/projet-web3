<tr>
    <td>
        <form action="/tasks/edit/<%= index %>" method="patch" hx-patch="/tasks/edit/<%= index %>" hx-target="#task-list" hx-swap="innerHTML">
            <input type="text" name="description" value="<%= task.description %>" required>
            <button type="submit">Enregistrer</button>
            <button type="button" hx-get="/tasks" hx-target="#task-list" hx-swap="innerHTML">Annuler</button>
        </form>
    </td>
</tr>
